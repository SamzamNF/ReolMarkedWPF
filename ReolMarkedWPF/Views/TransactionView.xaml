<Page x:Class="ReolMarkedWPF.Views.TransactionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="TransactionView"
      Background="{StaticResource BackgroundColor}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Salg og transaktioner" Style="{StaticResource PageHeader}" Grid.Row="0"/>

        <TabControl Grid.Row="1">
            <TabItem Header="Nyt salg">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,20,0">
                        <TextBlock Text="Vælg reol:" FontWeight="SemiBold" Margin="0,10,0,5"/>
                        <ComboBox ItemsSource="{Binding Shelves}" SelectedItem="{Binding SelectedShelf, Mode=TwoWay}" DisplayMemberPath="ShelfNumber"/>

                        <TextBlock Text="Vælg produkt:" FontWeight="SemiBold" Margin="0,20,0,5"/>
                        <ComboBox ItemsSource="{Binding ShelfProducts}" SelectedItem="{Binding SelectedProduct, Mode=TwoWay}" DisplayMemberPath="ProductName"/>

                        <Button Content="Tilføj til kurv" Command="{Binding AddToOrderDetailsCommand}" Style="{StaticResource CtaButton}" Margin="0,30,0,10"/>
                    </StackPanel>

                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Indkøbskurv" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>
                        <DataGrid Grid.Row="1" ItemsSource="{Binding TpVm.OrderDetails}" SelectedItem="{Binding TpVm.SelectedOrderDetail, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Produkt ID" Binding="{Binding ProductID}" Width="*" />
                                <DataGridTextColumn Header="Pris pr. stk." Binding="{Binding UnitPrice}" Width="*" />
                                <DataGridTextColumn Header="Antal" Binding="{Binding Amount}" Width="*" />
                            </DataGrid.Columns>
                        </DataGrid>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                            <Button Content="Fjern valgte" Command="{Binding RemoveFromOrderDetailsCommand}" Background="{StaticResource AccentColorSecondary}" Foreground="{StaticResource LightForegroundColor}" Margin="0,0,10,0"/>
                            <Button Content="Gennemfør salg" Command="{Binding AddTransactionCommand}" Style="{StaticResource CtaButton}"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Transaktionsoversigt">
            </TabItem>
        </TabControl>
    </Grid>
</Page>