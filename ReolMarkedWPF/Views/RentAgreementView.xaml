<Page x:Class="ReolMarkedWPF.Views.RentAgreementView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="RentAgreementView"
      Background="{StaticResource BackgroundColor}">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Lejeadministration" Style="{StaticResource PageHeader}" Grid.Row="0"/>

            <DataGrid Grid.Row="1" 
                      ItemsSource="{Binding RentAgreements}" 
                      SelectedItem="{Binding SelectedRentAgreement, Mode=TwoWay}" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      ColumnHeaderStyle="{StaticResource DataGridHeaderStyle}"
                      GridLinesVisibility="Horizontal"
                      Margin="0,0,0,15"
                      ScrollViewer.VerticalScrollBarVisibility="Disabled">
                <DataGrid.Columns>
                    <DataGridTextColumn 
                        Header="Aftale ID" 
                        Binding="{Binding RentAgreementID}" 
                        Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <StaticResource ResourceKey="DataGridStyle"/>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn 
                        Header="Startdato" 
                        Binding="{Binding StartDate, StringFormat=dd/MM/yyyy}" 
                        Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <StaticResource ResourceKey="DataGridStyle"/>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn 
                        Header="Slutdato"
                        Binding="{Binding EndDate, StringFormat=dd/MM/yyyy}" 
                        Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <StaticResource ResourceKey="DataGridStyle"/>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn 
                        Header="Lejer ID" 
                        Binding="{Binding ShelfVendorID}" 
                        Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <StaticResource ResourceKey="DataGridStyle"/>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Row="2" Background="#FFFFFF" CornerRadius="5" Padding="15">
                <StackPanel>
                    <TextBlock Text="Rediger valgt aftale" FontWeight="Bold" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Startdato:" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding StartDateText}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,10,0">
                            <TextBlock Text="Slutdato:" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding EndDateText}" />
                        </StackPanel>
                        
                        <Button Grid.Column="2" 
                                Content="Gem" 
                                Command="{Binding EditRentCommand}" 
                                Background="{StaticResource AccentColorSecondary}" 
                                Foreground="{StaticResource LightForegroundColor}" 
                                VerticalAlignment="Bottom" 
                                Margin="0,0,10,0"/>
                        
                        <Button Grid.Column="3" 
                                Content="Slet" 
                                Command="{Binding DeleteRentCommand}"
                                Background="{StaticResource DangerColor}" 
                                Foreground="{StaticResource LightForegroundColor}" 
                                VerticalAlignment="Bottom"/>

                        <Button Grid.Column="4"
                                Content="Scan alle aftaler"
                                Command="{Binding ClearRentAgreementsCommand}"
                                Background="{StaticResource DangerColor}"
                                Foreground="{StaticResource LightForegroundColor}"
                                VerticalAlignment="Bottom"
                                Margin="5,0,0,0"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Column="1" Background="#FFFFFF" CornerRadius="5" Padding="15">
            <StackPanel>
                <TextBlock Text="Ny lejeaftale" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="Opret en ny lejeaftale ved at trykke på knappen herunder." Margin="0,0,0,15" TextWrapping="Wrap" Foreground="{StaticResource MutedForegroundColor}"/>
                <Button Content="Opret ny lejeaftale" Command="{Binding MainVM.ShowCreateRentViewCommand}" Style="{StaticResource CtaButton}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>