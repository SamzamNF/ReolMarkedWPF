<Window x:Class="ReolMarkedWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReolMarkedWPF"
        mc:Ignorable="d"
        Title="ReolMarked MiddelBy" 
        MinHeight="450" 
        MinWidth="800" Foreground="Black" 
        WindowStartupLocation="CenterScreen" 
        WindowState="Maximized" 
        Cursor="Arrow">
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="Green" Offset="1"/>
            <GradientStop Color="White" Offset="0"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="DarkOliveGreen" Offset="1"/>
                <GradientStop Color="White" Offset="0.49"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="50" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- UI Start -->

        <!-- Første kolonne -->


        <Image Source="/Assets/Images/middelbylogo.jfif"
               Grid.Row="0"
               Grid.Column="0"
               HorizontalAlignment="Left"
               Stretch="Uniform"
               Margin="10"
               Height="150"/>

        <StackPanel Grid.Row="1" Grid.Column="0" VerticalAlignment="Top" Margin="10">
            <Separator Background="DarkRed" Height="2" Margin="0,0,0,10"/>


            <RadioButton Content="Lejere"
                             Height="50"
                             Foreground="Black"
                             FontSize="18"
                             FontWeight="Bold"
                             Style="{StaticResource MenuButtonTheme}"
                             Command="{Binding ShowShelfVendorViewCommand}"/>
            
            <RadioButton Content="Reoler"
                             Height="50"
                             Foreground="Black"
                             FontSize="18"
                             FontWeight="Bold"
                             Style="{StaticResource MenuButtonTheme}"
                             Command="{Binding ShowShelfViewCommand}"/>
            
            <RadioButton Content="Lejeaftaler"
                             Height="50"
                             Foreground="Black"
                             FontSize="18"
                             FontWeight="Bold"
                             Style="{StaticResource MenuButtonTheme}"
                             Command="{Binding ShowRentViewCommand}"/>

            <RadioButton Content="Salg"
                             Height="50"
                             Foreground="Black"
                             FontSize="18"
                             FontWeight="Bold"
                             Style="{StaticResource MenuButtonTheme}"
                             Command="{Binding ShowTransactionViewCommand}"/>



        </StackPanel>

        <!-- Anden kolonne -->

        <!-- Frame, hvori programmets undersider residerer -->

        <Frame x:Name="MainFrame"
               Grid.Row="1"
               Grid.Column="1"
               Grid.ColumnSpan="2"
               NavigationUIVisibility="Hidden"
               Margin="10"
               Height="Auto"
               Width="Auto"
               Content="{Binding CurrentView}" />
    </Grid>
</Window>
